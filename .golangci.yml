version: "2"

run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - revive

formatters:
  enable:
    - gofmt

linters-settings:
  revive:
    rules:
      - name: exported
        severity: warning
